# 네이버 블로그 모니터링 설정

이 파일만 고치면 스킬 동작이 바뀝니다. 표 형식(칸 순서, `|` 구분자)만 유지하면 자유롭게 행을 추가/삭제/수정해도 됩니다 — `scripts/fetch_rss.py`가 이 파일을 매번 읽어서 동작을 결정합니다.

## 블로그 목록

블로그를 추가/삭제하려면 이 표에 행을 더하거나 지우세요. `id`는 블로그 주소의 `blog.naver.com/{id}` 부분입니다.

| id | 이름 |
|---|---|
| up_6242 | 명예공인중개사사무소 |
| vertical3623 | M8 공장전문 명예공인중개사사무소 |
| ymy2020 | 공장다이쏘 다있는 명예부동산 |
| kwcho05 | 육각형공장-명예공인중개사사무소 |
| stay8566 | 명예공장 최다매물공장전문 |

## 관심 지역

포스팅 제목에서 찾을 지역명입니다. `코드`는 대시보드 코드(`assets/dashboard_template.html`)의 CSS 클래스/색상과 연결되므로, 지역을 추가할 땐 대시보드 쪽도 같이 손봐야 합니다 (아래 "지역 추가하기" 참고).

`매칭 키워드` 칸에 콤마로 여러 지역명을 넣으면 그 지역들이 하나로 합쳐집니다 — 예를 들어 천안과 아산을 한 카테고리로 보고 싶으면 `표시 이름`은 "천안·아산", `매칭 키워드`는 "천안,아산"으로 적으면 제목에 둘 중 하나만 있어도 이 카테고리로 잡힙니다. 이 칸을 비우면 `표시 이름` 자체를 키워드로 씁니다.

| 코드 | 표시 이름 | 매칭 키워드 |
|---|---|---|
| pt | 평택 | 평택 |
| as | 안성 | 안성 |
| ca | 천안·아산 | 천안,아산 |

## 기본 기준일

- 커버리지/우선순위: 7일
- 캘린더: 10일

숫자만 바꾸면 다음 실행부터 바로 적용됩니다. 한 번만 다르게 보고 싶을 땐 파일을 안 바꾸고 `--matrix-days`/`--calendar-days` 옵션으로 그때만 덮어써도 됩니다.

## 마지막 Artifact URL

https://claude.ai/code/artifact/75a7adc8-80c3-4f68-b366-0fbc7b6df28f

대시보드를 새로 발행할 때마다(URL이 없었거나, 기존 URL이 더는 유효하지 않을 때만) 이 줄을 새 URL로 갱신하세요. 평소에는 이 URL로 계속 갱신되어야 합니다.
