---
name: naver-blog-monitor
description: 사용자가 운영하는 여러 개의 네이버 부동산(공장·창고 임대) 블로그, 그리고 경쟁사 블로그들의 포스팅 현황을 관심 지역 기준으로 모니터링하고, 웹 대시보드(Artifact)로 보여준다. "블로그 현황 갱신해줘", "포스팅 확인해줘", "블로그 모니터링", "어느 블로그/도시에 써야 돼", "현황판 다시 만들어줘", "경쟁사 현황 갱신해줘", "경쟁사는 어디 쓰고 있어" 같은 요청이나, config.md/config_competitors.md에 등록된 블로그들 또는 관심 지역 포스팅 커버리지·빈도를 언급하면 반드시 이 스킬을 사용한다. 사용자가 기준일(며칠간)을 바꿔달라고 해도, 블로그나 관심 지역을 추가/삭제해달라고 해도, 새 경쟁사 블로그 주소를 보내며 같은 형식으로 봐달라고 해도 이 스킬로 처리한다.
---

# 네이버 블로그 포스팅 모니터링

사용자는 공장·창고 임대를 중개하는 부동산업자이고, 네이버 블로그 여러 개를 돌려가며 매물 포스팅을 올린다. 목적은 "어느 블로그가, 어느 지역 글을, 최근에 안 썼는지"를 한눈에 파악해서 오늘 뭘 써야 할지 정하는 것이다. 이 스킬은 그 판단에 쓰는 웹 대시보드를 새로 만들거나 최신 데이터로 갱신한다.

**블로그 목록, 관심 지역, 기본 기준일, 마지막으로 발행한 Artifact URL은 전부 [config.md](config.md)에 있다.** 이 파일이 실제 설정의 원본이니, 아래 지침에서 이 값들을 언급할 때는 하드코딩된 숫자가 아니라 항상 config.md의 현재 값을 가리키는 것으로 이해한다. 사용자가 "블로그 추가해줘", "이 지역도 보고 싶어", "기준일 바꿔줘"라고 하면 코드가 아니라 config.md를 고치면 된다 (지역 추가는 예외 — 아래 "새 지역 추가하기" 참고).

## 왜 이렇게 접근하는가

`blog.naver.com`, `rss.blog.naver.com` 도메인은 이 환경의 브라우저 정책상 WebFetch나 인앱 브라우저로 열리지 않는다(정책 차단이지 WAF가 아니라서 우회 시도 대상이 아니다). 대신 `curl_cffi`로 Safari TLS 지문을 흉내 내면 각 블로그의 RSS(`https://rss.blog.naver.com/{blogId}.xml`)는 문제없이 열린다. 이 패턴을 `scripts/fetch_rss.py`에 굳혀뒀으니, 매번 curl_cffi 명령을 새로 짜지 말고 이 스크립트를 실행한다.

## 실행 순서

### 1. 최신 데이터 가져오기

```bash
uv run --with curl_cffi python "<이 스킬 폴더>/scripts/fetch_rss.py" --out "<작업 디렉터리>/blog_data.json"
```

- 블로그 목록·관심 지역·기준일은 스크립트가 알아서 `config.md`에서 읽는다. 이번 한 번만 다르게 보고 싶을 때만 `--matrix-days N` / `--calendar-days N`으로 덮어쓴다(예: "이번만 3일로 봐줘") — config.md 자체는 건드리지 않는다. **반대로 앞으로 계속 다르게 보고 싶다("기준일을 5일 대신 3일로 바꿔줘")고 하면 config.md의 "기본 기준일" 숫자를 고쳐서 지속적으로 반영한다.**
- `uv`가 없으면 먼저 설치가 필요하다 (사용자에게 물어보고 진행: `irm https://astral.sh/uv/install.ps1 | iex` on PowerShell). 이미 이 스킬을 한 번 써봤다면 대개 설치돼 있다.
- 스크립트가 일부 블로그만 실패해도(예: 그 사이 blogId가 바뀜) 나머지는 정상 수집되고, 출력 JSON의 `errors` 배열에 실패한 블로그가 기록된다 — 있으면 사용자에게 알린다.
- 콘솔에 한글을 직접 출력하지 않는다(Windows cp949 콘솔에서 `UnicodeEncodeError`로 깨진다) — 스크립트는 이미 그렇게 짜여 있고, 결과는 파일로 받아 Read 도구로 읽는다.

### 2. 대시보드에 데이터 반영

`assets/dashboard_template.html`이 완성된 대시보드 뼈대다(디자인·CSS·필터 로직 전부 포함) — 매번 새로 디자인하지 말고 이걸 복사해서 데이터만 교체한다.

1. `blog_data.json`의 `last_artifact_url`(= config.md의 마지막 URL)을 `action: "read"`로 먼저 읽어서 최신 버전을 확보한다(다른 세션에서 갱신됐을 수 있음). URL이 없으면 `assets/dashboard_template.html`을 작업 디렉터리로 복사해서 시작한다.
2. `blog_data.json`을 읽고, 파일 안의 `// ===DASHBOARD_DATA_START===` ~ `// ===DASHBOARD_DATA_END===` 사이 블록(BLOGS/DAYS/TODAY/POSTS)만 새 데이터로 통째로 교체한다. 그 위·아래의 CSS, HTML 구조, `build*()` 함수는 건드리지 않는다 — 지금까지 다듬어 온 디자인이 그 안에 있다.
   - `BLOGS`: JSON의 `blogs`를 그대로 옮긴다.
   - `DAYS`, `TODAY`: JSON의 `days`, `today`를 그대로 옮긴다.
   - `POSTS`: JSON의 `posts`를 그대로 옮긴다(필드명 동일: blog/date/time/title/link/cities).
3. `MATRIX_WINDOW_DAYS` 상수(데이터 블록 밖, `buildMatrix` 함수 위쪽)를 JSON의 `matrix_window_days` 값으로 맞춘다. 이 값이 커버리지 매트릭스·우선순위 콜아웃의 날짜 범위를 정하고, 화면에 표시되는 날짜 범위 라벨도 여기서 자동 계산된다.
4. 헤더의 "캘린더 구간" 날짜 표시(`<span class="range">`)도 새 `days` 배열의 첫/마지막 날짜로 갱신한다.

**제목은 "MY Report"로 고정이다** (2026-09-12 사용자 요청 — 원래 "이번 판, 누가 어디를 비웠나"였다). `<h1>` 태그는 데이터와 무관하니 갱신할 때 건드리지 않는다.

### 3. 발행

`Artifact` 도구로 발행한다. **`blog_data.json`의 `last_artifact_url`이 있으면 반드시 그 URL을 `url` 파라미터로 넘겨서 같은 링크 위에 갱신한다** — 매번 새 Artifact를 만들면 사용자가 링크를 다시 찾아야 한다.

새로 발행했거나(URL이 원래 없었음), 기존 URL로 갱신이 안 됐다면(사용자가 지웠거나 다른 세션에서 못 찾음) 새 URL을 **config.md의 "마지막 Artifact URL" 줄에 고쳐 써서** 다음 번에도 같은 링크가 이어지게 한다.

## 지역 분류 규칙

config.md "관심 지역" 표는 `코드 | 표시 이름 | 매칭 키워드` 3칸이다. 제목에 `매칭 키워드`(콤마로 여러 개 가능) 중 하나라도 들어가면 그 코드로 태깅한다. 한 제목에 서로 다른 코드의 키워드가 같이 언급되면(예: "평택 화성 안성 천안...") 전부 태깅한다 — 대시보드에서 그 글이 여러 지역 칸에 동시에 카운트되는 게 맞다. 등록된 키워드가 하나도 없으면(관심 밖 지역, 또는 노하우성 글) `기타`로 분류한다.

**여러 지역을 한 카테고리로 묶기**: 2026-09-07 사용자 요청으로 천안과 아산을 한 카테고리(`ca`, 표시는 "천안·아산")로 묶었다 — `매칭 키워드` 칸에 "천안,아산"을 넣으면 제목에 둘 중 하나만 있어도 이 코드로 잡히고, 두 지역이 같이 있는 하나의 글이 두 번 카운트되지도 않는다(코드 하나에 여러 키워드가 매칭돼도 결과 코드는 한 번만 붙는다). `config.md`와 `config_competitors.md`는 반드시 같은 키워드 매핑을 유지해야 한다 — 둘이 어긋나면 우리 블로그 현황판과 경쟁사 현황판을 나란히 비교하는 의미가 없어진다.

## 새 지역 추가하기

config.md의 "관심 지역" 표에 새 행(코드/표시 이름/매칭 키워드)을 추가하는 것만으로는 대시보드에 반영되지 않는다 — `assets/dashboard_template.html`은 각 지역 코드마다 전용 CSS 색상(라이트·다크 둘 다)과 `CITY_META`/`CITY_ORDER`/범례 항목을 갖고 있어서, 새 코드를 추가하면 이 부분들을 같은 패턴으로 하나씩 늘려야 한다. 기존 pt/as/ca 항목이 그대로 예시가 된다. (블로그 추가/삭제는 반대로 config.md만 고치면 끝 — 코드 쪽은 블로그 개수에 상관없이 동작한다.)

## 우선순위 판단 로직 (참고용 — 코드에 이미 구현됨)

- config.md에 등록된 관심 지역 중 최근 `MATRIX_WINDOW_DAYS`일간 포스팅이 0건인 블로그×지역 조합을 전부 뽑아 "여기부터 채우세요"로 제시한다.
- 같은 기간 전체 포스팅 수가 가장 많이 쓴 블로그의 절반 이하인 블로그는 "페이스가 느림"으로 따로 짚는다.
- 사용자가 결과를 보고 실제로 뭘 쓸지 정하는 것이 목적이므로, 이 판단은 힌트일 뿐 결론이 아니다 — 대시보드 자체(매트릭스, 캘린더, 필터 가능한 목록)를 보고 사용자가 최종 판단한다.

## 경쟁사 버전 (별도 대시보드)

우리 블로그와 별개로, **경쟁사** 블로그들의 같은 지표를 보여주는 두 번째 대시보드가 있다. 구조(매트릭스/캘린더/상세 목록/필터)는 동일하지만, 데이터 원본과 인사이트 로직이 다르므로 완전히 분리된 파일 세트를 쓴다:

- 설정: [config_competitors.md](config_competitors.md) — `config.md`와 같은 형식, 대상만 경쟁사 블로그. 새 경쟁사가 생기면 여기 표에 행을 추가한다(이름 칸은 비워도 됨 — `fetch_rss.py`가 RSS 채널 제목으로 자동 채운다).
- 템플릿: `assets/competitor_dashboard_template.html` — 우리 블로그 템플릿과 이제 구조 자체가 다르다 (2026-09-07 사용자 요청으로 "지역 커버리지" 매트릭스 섹션을 완전히 뺐다):
  1. **지역 커버리지 매트릭스 섹션이 없다.** 경쟁사×지역 표를 보여주는 대신 곧바로 "지역별 경쟁 강도" 요약으로 시작한다 — 경쟁사가 20곳이 넘어가면 블로그별 매트릭스보다 지역 단위 집계가 더 읽기 쉽다는 판단.
  2. **"지역별 경쟁 강도"**(`buildCompetitiveSignal` + `computeStats` 함수)는 `config_competitors.md`의 캘린더 기준일(현재 10일) 전체를 그대로 집계한다 — 매트릭스가 있던 이전 버전은 이 부분만 더 짧은 기준일(5일)을 따로 썼지만, 지금은 별도 윈도우가 없다. `POSTS` 배열 자체가 이미 `fetch_rss.py`가 캘린더 기준일만큼만 가져온 결과라서, 코드에서 다시 날짜로 잘라낼 필요가 없다 — `DAYS.length`가 곧 그 기준일 수다. **캘린더 기준일을 바꾸면 이 집계 기간도 같이 바뀐다** (독립된 기준일이 아니다 — 우리 블로그 대시보드의 5일/10일 분리와는 다른 점).
  3. 어느 지역이 가장 조용한지(=기회)/가장 붐비는지(=경쟁 치열)와 "가장 활발한 경쟁사 Top 3"를 보여준다. 개별 경쟁사×지역 빈칸을 "채우세요"라고 말하는 건 의미가 없다(그건 우리가 결정할 수 있는 게 아니다) — 그래서 이 부분만 우리 블로그 쪽과 로직이 다르다.
  4. 맨 위에 우리 블로그 현황판으로 가는 `.companion-nav` 버튼이 있고, 우리 블로그 현황판 쪽에도 이 경쟁사 현황판으로 오는 버튼이 있다 — **두 대시보드는 서로의 URL을 하드코딩해서 링크한다.** 어느 한쪽 URL이 바뀌면(예: 사용자가 지워서 새로 발행) 반드시 반대쪽 파일의 `.companion-nav` 링크도 새 URL로 고쳐서 republish해야 링크가 안 끊긴다.
- 실행 순서는 동일하다: `fetch_rss.py --config config_competitors.md`로 데이터를 받고, 템플릿의 데이터 블록을 교체하고, `config_competitors.md`의 마지막 URL로 갱신 발행한다.

가장 최근 경쟁사 현황판 URL: `https://claude.ai/code/artifact/2d35938e-cdbb-4020-8559-918e1519c940` (실제 최신값은 `config_competitors.md`를 확인 — 여기 적힌 값은 참고용이고 그 파일이 원본이다.)

**경쟁사 쪽만 지역이 3개 더 많다**: 2026-09-09 사용자 요청으로 `config_competitors.md`에 화성(hs)·오산(os)·용인(yi)을 추가해서, 경쟁사 현황판은 평택/안성/천안·아산/화성/오산/용인 6개 지역을 구분한다. `config.md`(우리 블로그)는 아직 3개(평택/안성/천안·아산)뿐이다 — "config.md와 config_competitors.md는 같은 키워드 매핑을 유지해야 한다"는 위 원칙과 지금은 의도적으로 다른 상태다(경쟁사가 우리보다 넓은 지역에서 활동해서 세분화가 더 필요했음). 우리 블로그 쪽에도 화성/오산/용인을 추가해달라는 요청이 오면 `config.md`에 같은 3행을 추가하고 `assets/dashboard_template.html`에도 동일하게 CSS 색상·CITY_META·매트릭스 열·범례를 추가한다.

`assets/competitor_dashboard_template.html`의 JS는 `CITY_ORDER`를 `CITY_META`에서 직접 뽑지 않고 배열로 하드코딩하지만, `computeStats`/`buildCompetitiveSignal`은 `CITY_ORDER` 배열 하나만 보고 totals·순위를 계산하도록 일반화돼 있다 — 그래서 지역을 추가/삭제할 때 `CITY_META`와 `CITY_ORDER` 두 곳만 맞추면 되고, 로직 함수 내부는 건드릴 필요가 없다. (반대로 `assets/dashboard_template.html`은 아직 이 일반화가 안 돼 있어서 `buildMatrix`/`buildPriority` 안에 `{pt:0,as:0,ca:0,etc:0}` 같은 고정 객체가 남아 있다 — 우리 블로그 쪽에 지역을 추가할 땐 이 부분도 같이 고쳐야 한다.)
